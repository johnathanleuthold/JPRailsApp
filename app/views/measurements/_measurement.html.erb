<!-- Displays a single ingredient -->
<tr class="measurement">
  <td><%= measurement.name %></td>
  <td><%= measurement.abbr %></td>
  <td>
    <%= link_to 'edit', edit_measurement_path(measurement) %>
    <%= link_to 'delete', measurement_path(measurement), method: :delete, 
                          data: { confirm: 'Are you sure?' } %></td>
  </td>
</tr>